# Googleログインを有効にする手順

**まだ有効になっていません。** いま入っているのは受け皿だけで、設定シートが空のあいだは
今までどおり合言葉だけで動きます。壊れていないことは実測で確認済みです。

有効にすると合言葉は不要になり、許可したGoogleアカウント以外は数字を1つも取れなくなります。

---

## なぜ必要か

リポジトリが Public なので、GASのURLは誰でも読めます。
いま守っているのは `banchou` という合言葉ひとつだけで、これは推測されます。

## なぜJIROKUと同じ作りにしなかったか

JIROKUは招待制で数十人が使うため、自前セッション・監査ログ・ワンタイムコードまで持っています。
このアプリは1〜数人なので、**セッションを持たず、GoogleのIDトークンをその都度Googleに確認する**
だけにしました。セッションを持つと、期限切れ・失効・端末間のズレを自分で面倒みることになります。

---

## Kaiの作業（3ステップ）

### ① Googleでクライアントを作る

1. https://console.cloud.google.com/apis/credentials を開く
2. プロジェクトが無ければ作る（名前は何でもよい）
3. **認証情報を作成 → OAuth クライアント ID**
4. アプリケーションの種類：**ウェブ アプリケーション**
5. **承認済みの JavaScript 生成元** に、アプリを開くURLの**オリジンだけ**を入れる

```
https://kaisunagawa.github.io
```

6. 作成すると `〜.apps.googleusercontent.com` という**クライアントID**が出る。これをコピー

※ 同意画面の設定を求められたら、User Type は「外部」、アプリ名は「せつやく番長」、
サポートメールは自分のアドレスでよい。公開せず「テスト」のままでも、テストユーザーに
自分を追加すれば使えます。

### ② 2か所に貼る

**(a) `index.html`**

```js
const GOOGLE_CLIENT_ID = '';   // ← ここに貼る
```

**(b) スプレッドシートの「設定」シート**

| 項目 | 値 |
|---|---|
| GoogleログインのクライアントID | ①でコピーしたID |
| ログインを許可するメール | `work.sunagawa@gmail.com` |

**「ログインを許可するメール」は必ず入れてください。** 空だと、そのクライアントIDで
発行されたログインなら誰でも通ってしまいます。

### ③ 出る道まで踏んでから本番へ

有効にすると、しくじったとき自分も入れなくなります。順番に踏んでください。

1. ログインできる
2. 一度ログアウトして、**もう一度ログインできる**
3. 別のGoogleアカウントでは**拒否される**
4. スマホのSafariでも1〜3ができる

万一入れなくなったら、**スプレッドシートの「GoogleログインのクライアントID」を空にすれば**
合言葉での入口に戻ります。スプレッドシートはGoogleアカウントで直接開けるので、
アプリからログインできなくても必ず戻せます。

---

## 有効にしたあと

- 合言葉は不要になります（URLの `?k=` も不要）
- IDトークンは1時間で切れます。切れたらログイン画面が出るので、押し直すだけです
- トークンは端末のsessionStorageにだけ持ちます。タブを閉じれば消えます
